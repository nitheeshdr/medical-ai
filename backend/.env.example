MONGODB_URI=mongodb://localhost:27017/medinova
JWT_SECRET=your_jwt_secret_minimum_32_chars_here
JWT_REFRESH_SECRET=your_refresh_secret_minimum_32_chars_here

# NVIDIA NIM (preferred) — get key at https://build.nvidia.com/
NVIDIA_API_KEY=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
NVIDIA_MODEL=meta/llama-3.1-70b-instruct

# OpenAI fallback (used if NVIDIA_API_KEY is not set)
OPENAI_API_KEY=sk-...

NEXT_PUBLIC_API_URL=http://localhost:3001

# Firebase Admin SDK — for FCM push notifications
# Download your service account key from:
# Firebase Console → Project Settings → Service Accounts → Generate new private key
# Then paste the entire JSON as a single-line string (or use a tool to minify it)
FIREBASE_SERVICE_ACCOUNT_KEY=
